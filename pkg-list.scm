(package (fectors (0 1))
  (synopsis "Functional Vectors in Scheme")
  (description
   "Functional Vectors in Scheme. Semi persistent, to allow fast
updates to most recent version.  Not thread safe.")
  (homepage "http://github.com/ijp/fectors")
  (libraries "fectors.sls"))
